alert("le fichier JS est exécuté!");

document.getElementById("<%= @task.category.title %>-tasks").insertAdjacentHTML("beforeend", "<%= j render 'task', task: @task %>");
console.log("ajout nouvelle tache en temps réel");
